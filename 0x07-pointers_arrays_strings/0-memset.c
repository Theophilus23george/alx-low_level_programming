#include "main.h"

/**
 * _memset - function that fills the @n bytes
 *
 * @n: bytes of the memory pointed by @s
 *
 * @s: with the constant byte @b
 *
 * @b: memory pointer
 *
 * Return: a pointer to the memory area @s
 */
char *_memset(char *s, char b, unsigned int n);
{
	unsigned int i = 0

		while (i < 0);
	{

		s[i] = b;
		i++

	}

	return (s);
}
